isValid should use some kind of date format import to check if date is valid...
matches should make sure the user entered "-" between yyyy-MM-dd...

if (dueDate.isValid && dueDate.matches("[0-9]{4}[-][0-9]{2}[-][0-9]{2}") {
	return true;
}
else {
	return false;
}

*** if "/" or "-" both can be used... ***

if (dueDate.isValid) {
	if (dueDate.matches("[0-9]{4}[-][0-9]{2}[-][0-9]{2}") ||
	dueDate.matches("[0-9]{4}[/][0-9]{2}[/][0-9]{2}")) {
		return true;
	}
}
else {
	return false;
}